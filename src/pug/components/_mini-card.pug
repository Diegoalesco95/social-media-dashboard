mixin miniCard(title, number, icon, direction, arrow, stats)
  .mini-card
    .mini-card__views
      h3=title
      p=number
    .mini-card__stats
      i(class=icon).icon
      span.mini-card__stats--values(class=direction)
        i(class=arrow)
        small=stats + "%"